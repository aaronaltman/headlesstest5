// components/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#000066', // You can use a custom color or a CSS variable here
        },
        secondary: {
            main: '#0969da', // You can use a custom color or a CSS variable here
        },
    },
});

export default theme;
